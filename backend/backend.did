service : {
  addItem: (text) -> ();
  deleteItem: (nat) -> ();
  getItems: () -> (vec record {
                         nat;
                         text;
                         bool;
                       }) query;
  toggleItem: (nat) -> ();
}
